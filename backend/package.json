{
  "name": "mindease-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Local MindEase wellness assistant backend using Express and the OpenAI API.",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check routes/chat.js && node --check services/openai.js && node --check prompts/mindeaseSystem.js && node --check templates/crisisResponse.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.18.3",
    "openai": "^4.104.0"
  }
}
